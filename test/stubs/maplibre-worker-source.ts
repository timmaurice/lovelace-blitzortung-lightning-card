// Stands in for the worker source rollup inlines at build time (see rollup.config.js). The unit
// tests only need `installMapLibreWorker` to have something it can put into a Blob.
export default '/* MapLibre worker source stub */';
